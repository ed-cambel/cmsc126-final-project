// edit page - can edit a user's profile info: name, email, password, etc.
// redirects from profile page, redirects to profile page after submission
// can only be viewed by user

export default function EditProfilePage() {
    return <h1>Edit Profile Page</h1>;
}