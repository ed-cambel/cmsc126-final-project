// spot edit pages - can edit a study spot's info: name, location, description, etc.
// redirect from spot page, redirect to spot page after submission
// can only be viewed by user

export default function EditSpotPage() {
    return <h1>Edit Spot Page</h1>;
}